import { SvgIcon } from "@mui/material";
import ValueSvg from "static/performance/value.svg";

const ValueIcon = (props) => (
  <SvgIcon aria-label="Value" component={ValueSvg} {...props} />
);

export default ValueIcon;
