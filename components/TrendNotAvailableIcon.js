import { SvgIcon } from "@mui/material";
import TrendNotAvailableSvg from "static/trends/not-available-icon.svg";

const TrendNotAvailableIcon = (props) => (
  <SvgIcon component={TrendNotAvailableSvg} {...props} />
);

export default TrendNotAvailableIcon;
